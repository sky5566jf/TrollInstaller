# Theos build output
.theos/
Payload/
*.tipa
obj/

# Source asset (AppIcon.png is the generated build asset)
mh*.png

# Editor / OS noise
.DS_Store
*.swp

# WorkBuddy internal memory (not part of the app)
.workbuddy/

# Local diagnostic / helper artifacts (not part of the app)
ghlog*.txt
dl_artifact.py
inspect_tipa.py
monitor_build.py
validate_tipa.py
